packages:
  "recharts": "For dashboard statistics charts"
  "react-hook-form": "For robust form handling"
  "@hookform/resolvers": "Zod validation resolver for forms"
  "date-fns": "For formatting dates in the credentials list"
  "clsx": "Utility for constructing className strings conditionally"
  "tailwind-merge": "Utility for merging tailwind classes safely"

images: []

notes:
  - Using generated hooks from @workspace/api-client-react
  - Implementing brutalist, flat geometric design with strong borders and no shadows
  - Auth relies on session cookies managed transparently by customFetch
